# Page topology

Hash SPA (Vue Router) cloned as Next.js pathnames.

1. Login overlay (full viewport) — no chrome
2. Admin shell: logo + dark sidebar (fixed 240px) + header 64px + tags 40px + scrolling view `#f0f2f5`
3. Dashboard metric row + two charts
4. Config forms (base / pay / page)
5. CRUD tables with dialogs for every remaining menu item listed in OUTPUT_PLAN.md
